/* global angular:false */

(function () {
    "use strict";

    angular.module("jsnbt")
        .controller('UsersController', function ($scope, $rootScope, $location, $route, $timeout, $q, $logger) {
            
            var logger = $logger.create('UsersController');

        });
})(); 