// validate domain (in installed domains)
// validate list (in installed lists)
// validate localization (if enabled, validate data.localization structure, else set to en and define data.localization.en object);